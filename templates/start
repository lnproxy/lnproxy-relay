{{template "head"}}

<form action="/wrap" method="POST">
	<textarea
			name="body"
			autocomplete=off
			autocorrect=off
			autofocus=true
			placeholder="lnbc..."
			required=true
			rows=9
			spellcheck=false
		></textarea>
	<button name="wrap">Wrap</button>
</form>

{{template "foot"}}
